apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  name: logrotate
  labels:
    app: logrotate
spec:
  template:
    metadata:
      labels:
        app: logrotate
    spec:
      containers:
      - name: logrotate
        image: supergiant/logrotate:latest
        imagePullPolicy: Always
        volumeMounts:
        - name: host
          mountPath: /host
      volumes:
      - name: host
        hostPath:
          path: /
